<div class="flex justify-center items-center w-10 h-12 flex-wrap">
  <div class="rounded shadow">
    <img
      class="w-full h-full object-cover"
      src="{{ collection.featured_image }}"
      alt="{{ collection.title }}">
  </div>
  <div>
    <p>{{ collection.title }}</p>
  </div>
</div>